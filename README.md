1- if thare tre same node deplucated in same position and u just get the osm frm openstretmap so u need to use use fix_osm_rouadrunner_to_json.py 
2- open it in vector map builder web site and edit it and see if it exist same rout need to change derection or to detect the missiong connect if one route is not autoetect as the next line of the first one so u need to overlay the start point of u r route and the end point of the secend one and erun the code to delet the overlay 
to adjast the speed use json 
3- to check if thare is same route with one node just check the log invector map bulder or use the code verification_map.py and to fix it just delet the noude from vector map builder and fix connection if need like mantion in step 2
